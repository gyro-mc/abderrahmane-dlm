module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['var(--font-sans)'],
        mono: ['var(--font-mono)'],
        allura: ['var(--font-deco)'], // for when you want to use Allura
      },
    },
  },
};
